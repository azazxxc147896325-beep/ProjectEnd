{
  "name": "@campus-food/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript interfaces, types and enums for Campus Food Ordering System",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
